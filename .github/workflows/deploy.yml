  - name: Build Docker image
  run: |
    docker build -t booksystemregistry.azurecr.io/book-system:latest ./BookSystem
  
  - name: Push Docker image
  run: |
    docker push booksystemregistry.azurecr.io/book-system:latest

